#! /bin/sh
for modpath in debian/tmp/petsc${PETSC_SONAME_VERSION}-complex/${PETSC_COMPLEX_DIR_PREFIX}/include/*.mod; do
  modfile=$(basename $modpath)
  echo ${modpath} ${PETSC_COMPLEX_DIR_PREFIX}/include
done
