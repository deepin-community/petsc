#! /bin/sh
for modpath in debian/tmp/petsc${PETSC_SONAME_VERSION}-real/${PETSC_REAL_DIR_PREFIX}/include/*.mod; do
  modfile=$(basename $modpath)
  echo ${modpath} ${PETSC_REAL_DIR_PREFIX}/include
done
